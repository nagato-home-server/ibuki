# Daily 20260912

## 実施内容

- GitHub の最新変更を fast-forward で取得し、GRE over IPsec 対応を含む `491f7e0` を作業ツリーへ反映した。
- `samples/gre-over-ipsec.yaml` を使い、GRE over IPsec の経路選択、VPP GRE トンネル、strongSwan transport mode、GRE セレクタ、ロールバック計画を確認した。
- CMake ビルド後に `eventnet_tests` と `eventnet_gre_over_ipsec_plan` を実行し、全テストに成功した。
- `--out-dir out/gre-custom` を指定した生成も確認し、生成された apply/integrated スクリプトが指定先の計画ファイルを参照することを確認した。
- 任意の出力先を指定したときも `out/netns-runtime` を参照してしまう不具合を修正した。apply スクリプト、integrated スクリプトの VPP 計画と GRE strongSwan 設定の参照先を出力先から組み立てるようにした。
- GRE の CMake 回帰検査が絶対パスの一時出力先でも成立するよう検査条件を一般化した。
- Ubuntu VMへstrongSwanとVPPを導入し、VPP 26.06-releaseの起動と`vppctl`接続を確認した。
- VM上でdirect IPsecを実行し、IKE_SA/CHILD_SAの確立、LAN間ping、ESP送信カウンタ増加まで確認した。

## 難しかったこと・停滞したこと

- GRE の計画生成と静的なスクリプト検査は完了したが、WSL 環境には `vpp`、`vppctl`、`swanctl` がないため、実際の VPP 転送および strongSwan ネゴシエーションは実行できなかった。
- したがって、今回確認できたのは C の生成処理、設定内容、コマンド列、ロールバック計画までであり、GRE パケットが実際に IPsec で保護されて通過することは未検証である。

## 次回

- VPP と strongSwan が利用できる Linux VM で GRE over IPsec の実ランタイムを検証する。
- GRE トンネル生成、strongSwan SA 確立、VPP 経路、LAN 間疎通、ESP カウンタ、異常時ロールバックを順に確認する。

## 実環境で確認できたこと

- `eventnet_tests`、GRE計画テスト、Cコンパイルは成功した。
- strongSwanは`site-a`と`site-b`のnamespace内で起動し、`tun-a-b`のCHILD_SAが確立した。
- `10.10.1.0/24`から`10.10.2.0/24`への暗号化pingが成功し、ESP out packetsが`0 -> 3`に増加した。
- VPPは`/usr/bin/vpp`、`/usr/bin/vppctl`で利用可能で、systemdサービスもactiveになった。
- 未導入のstrongSwanプラグインに関する警告は出たが、今回のPSK・AES・kernel-netlink・VICI経路には影響せず、SA確立は成功した。
- GRE実ランタイムでは、VPP 26.06で`set interface mtu gre0 1400`が拒否されることを確認した。VPPの正しい形式`set interface mtu 1400 gre0`へレンダリングを修正した。
- GRE統合スクリプトは、namespace内のcharon/VICI起動とGRE用underlayアドレス設定が未接続であり、グローバルVICIへ接続して設定を読み込もうとして失敗した。これは次の実装課題として切り分けた。
- GRE実統合を実装した。YAMLにIKE endpointとVPP GRE outer endpointを分離する項目を追加し、namespace内charonの起動、site-b用設定の反転生成、VICIによる設定ロードとGRE CHILD_SA開始、VPP underlay route、GRE生成、rollback停止を統合スクリプトへ接続した。
- `eventnet_tests`と`eventnet_gre_over_ipsec_plan`を再実行し、全テストに成功した。追加shellの構文検査も成功した。
- VMでGRE実統合を再実行し、namespace内charon起動、VICI設定ロード、GRE transport CHILD_SA確立（`203.0.113.10/32[gre] === 203.0.113.9/32[gre]`）まで成功した。
- その後のVPP GRE作成は、前回実行の`gre0`が残っていたため`Instance is in use`で失敗した。生成VPP planの開始時とrollback時にGRE tunnelを明示削除する修正を追加した。
