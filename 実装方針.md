標準Tunnelの抽象化と状態遷移制御に基づくイベント駆動型ネットワーク制御基盤「Ibuki」の設計
要旨
企業ネットワークや拠点間ネットワークでは、IPsec Tunnel、ルーティング、パケット転送、ネットワーク品質監視など、複数のネットワーク機能を組み合わせることで通信経路を構成している。strongSwan、FD.io VPP、FRRouting（FRR）などの既存OSSを利用すれば、VPN、転送、ルーティングといった個々の機能をオープンソースソフトウェアのみで構築することは可能である。一方で、複数のTunnelから構成される通信経路について、どの経路を利用するかを決定し、現在利用中の経路から別の経路へ通信への影響を考慮しながら移行させる処理は、各OSSの個別の責務を超える。
本研究では、標準的なTunnelを共通のネットワークリソースとして抽象化し、異なるTunnel実装や転送機構に対して統一的な経路選択および状態遷移制御を適用する、イベント駆動型ネットワーク制御基盤「Ibuki」を提案する。Ibukiは、新しいVPNプロトコルや独自の暗号方式、独自データプレーンを開発するものではない。既存のIPsec、ルーティング、パケット転送機構を利用し、その上位に共通の制御モデルを構築することを目的とする。
本研究の新規性は二点に整理する。第一に、ネットワーク機器全体ではなく、拠点間に形成されるTunnelを共通制御リソースとして扱うことである。実装固有の処理をAdapterへ局所化し、それぞれの実装が利用可能な機能をCapabilityとして上位層へ公開することで、Path SelectionやPath Transitionの中核ロジックが特定の製品やVPN実装へ強く依存しない構造を目指す。第二に、Path変更を単なる最終設定の更新として扱うのではなく、準備、検証、切替、切替後確認、失敗時復旧という中間状態を持つTransitionとして扱うことである。これにより、通信継続性、順序安定性、切替速度など、異なる要求に応じてTransition Strategyを選択できるようにする。
本稿では、Ibukiの背景、課題設定、リソースモデル、状態モデル、Intent、Path Selection、Path Transition、管理ドメイン間協調、Explain機構を整理し、ONOS、Kubernetes、flexiWANとの比較を通じて本研究の位置付けを明確にする。また、提案モデルの有効性を検証するための評価環境および評価方法について述べる。

1. はじめに
近年、ネットワーク機能のソフトウェア化が進み、従来は専用ネットワーク機器の内部に実装されていた多くの機能を、汎用的な計算機とOSSを用いて構築できるようになっている。
例えばstrongSwanを利用すれば、IKEv2/IPsecに基づく暗号化Tunnelを構成できる。また、FD.io VPPを利用すれば、高性能なソフトウェアパケット転送を実現できる。さらにFRRoutingを利用することで、BGPやOSPFなどの標準的な動的ルーティングプロトコルを利用できる。
このように、VPN、パケット転送、ルーティングという個別機能については、すでに成熟したOSSが存在している。そのため、OSSを用いて拠点間VPNを構築すること自体は、新しい研究課題とは言い難い。
しかし、実際のネットワーク運用では、単にTunnelが存在し、経路が設定されているだけでは十分ではない。複数の通信経路が利用可能な場合、どの経路を選択するかを判断する必要がある。また、現在利用中の経路から別の経路へ切り替える際には、通信断やパケット損失、パケット順序の変化、既存セッションへの影響などを考慮する必要がある。
例えば、拠点Aと拠点Bの間にDirect経路、Hub経由、Relay経由という複数の経路が存在する場合を考える。Direct経路が利用可能であれば、それを優先して利用したい場合がある。一方でDirect経路の品質が悪化した場合にはHub経由またはRelay経由へ切り替える必要がある。また、セキュリティポリシー上、特定のGatewayを必ず経由させたい場合もある。
このとき重要なのは、「どの経路を利用するか」という選択だけではない。「現在利用している経路から、選択された新しい経路へどのように移行するか」という問題も存在する。
例えば、新しいTunnelを確立する前に現在の経路を削除すれば、一時的に利用可能な経路が存在しない状態が生じる。一方、新しい経路へ直ちに切り替えた場合には、旧経路を通っているパケットと新経路を通る後続パケットの到着順序が入れ替わる可能性がある。また、既存のTCPセッションに影響を与える可能性もある。
したがって、動的なネットワーク制御では、経路選択だけでなく、経路変更そのものを状態遷移として扱う必要がある。
さらに、strongSwan、VPP、FRRなどの既存OSSは、それぞれ異なる設定方法、API、状態モデルを持っている。strongSwanではIKE SAやCHILD SA、VPPではForwarding State、FRRではRouting Stateというように、それぞれが独立した状態を管理している。
これらを統合するために個別のスクリプトを作成することも可能であるが、その方法では特定の構成やソフトウェアへ依存しやすくなる。また、拠点数や候補経路数が増加した場合に、状態管理や障害復旧処理が複雑になる。
そこで本研究では、標準的なTunnelを共通の制御対象として抽象化し、複数Tunnelから構成される通信Pathについて、選択、準備、検証、遷移、確認、復旧を一貫して扱う制御基盤「Ibuki」を提案する。

2. 研究背景
2.1 標準IPsecの利用
Ibukiでは、新しいVPN方式を導入せず、既存のIPsecおよびIKEv2を利用する。
IPsecはIPパケットに対する認証、完全性、機密性などのセキュリティ機能を提供する標準技術であり、企業向けVPN製品でも広く利用されている。IKEv2は、通信相手の認証やSecurity Associationの確立・維持を行う。
本研究では、これらの標準を変更しない。独自のESPフォーマット、独自の暗号化方式、Tunnelをまたぐ独自シーケンス番号、パケット複製機構、受信側での独自並べ替え機構などは導入しない。
これは、本研究の目的がVPNプロトコルそのものの改良ではなく、既存のTunnelをどのように制御するかという上位レイヤの問題にあるためである。
2.2 strongSwan
strongSwanは、IKEおよびIPsecを実装する代表的なOSSの一つである。strongSwanにはVICIと呼ばれる制御インターフェースがあり、外部アプリケーションからIKE daemonの設定、制御、状態監視を行うことができる。
このため、IbukiではstrongSwanを変更するのではなく、Adapterを通じて制御対象として利用する。Tunnelの設定、確立、状態取得、削除などはstrongSwan Adapterが担当し、上位のPath SelectionやTransition EngineはstrongSwan固有の詳細を直接扱わない構造とする。
2.3 FD.io VPP
FD.io VPPは、高性能なソフトウェアパケット転送基盤である。Ibukiでは、Tunnelが存在することと、そのTunnelを利用して実際にパケットを転送することを分離して扱う。
例えばDirect TunnelとHub Tunnelが同時に確立されていても、実際の通信をどちらへ転送するかは別途決定される。そのため、VPPはIbukiにおけるForwarding Planeの実装候補となる。
2.4 FRRouting
FRRoutingは、BGP、OSPF、IS-ISなど複数のルーティングプロトコルを実装するOSSルーティングスイートである。
Ibukiの初期設計では、Pathを明示的に管理する場合にFRRを必須とはしない。一方で、将来的に動的ルーティング情報をPath Selectionへ取り込む場合には、FRRを観測対象または制御対象として利用できる。
したがってFRRは、Ibukiの中核そのものではなく、外部のRouting StateをPath制御へ反映するための拡張可能なコンポーネントとして位置付ける。

3. 課題設定
3.1 Tunnelの確立と利用は異なる
VPNソフトウェアを利用すれば、Tunnelを確立することはできる。しかし、Tunnelが確立していることと、そのTunnelを現在の通信経路として利用することは同一ではない。
例えば、Direct Tunnelが確立済みであっても、Packet Lossが大きい場合や、セキュリティ上の制約を満たしていない場合には、そのTunnelを利用すべきではない。
逆に、現在Hub経由で通信していても、Direct Tunnelが正常になった場合には、Directへ移行した方が望ましい場合がある。
そのためIbukiでは、Tunnelそのものの状態と、Tunnelを組み合わせて構成されるPathの利用状態を分離して管理する。
3.2 経路変更には中間状態が存在する
ネットワーク制御において、最終的なDesired Stateだけを管理すればよいとは限らない。
例えば、現在Hub経由で通信しており、目標状態としてDirect Pathを利用したい場合を考える。安全に切り替えるためには、まずDirect Tunnelを準備し、そのTunnelが利用可能であることを確認し、必要なForwarding Stateを用意したうえで、通信を新経路へ移す必要がある。
さらに、切替後に通信可能であることを確認しなければならない。もし検証に失敗した場合には、以前のHub Pathへ戻せる必要がある。このように、現在状態から目標状態への間には複数の中間状態が存在する。
Ibukiでは、この中間状態をTransition Stateとして明示的に扱う。
3.3 機器全体の抽象化は対象としない
ネットワーク装置全体を共通化しようとすると、Interface、VLAN、NAT、Firewall、ACL、QoS、Routing Protocol、Hardware Offloadなど多数の差異を扱う必要がある。
Ibukiでは、これらすべてを統一的に管理することを目的としない。
制御対象を、拠点間に形成されるTunnelと、それらを構成要素とするPathに限定する。
これにより、実装固有の処理を比較的小さなAdapterへ閉じ込め、上位の制御ロジックが機器固有の実装へ依存しすぎない構造を目指す。
3.4 制御判断の説明可能性
動的なネットワーク制御では、現在どの経路が利用されているかだけでなく、なぜその経路が選択されたのかを確認できることが重要である。
例えばDirect PathからRelay Pathへ切り替わった場合、その原因がDirect Pathの障害なのか、Packet Lossの増加なのか、管理者が設定したPriorityなのか、別DomainのPolicyによる拒否なのかを確認できなければ、運用者は障害原因を特定しにくい。
そのためIbukiでは、Path SelectionおよびPath Transitionの判断理由をDecision Logとして保持し、Explainとして表示可能にする。

4. 研究目的と新規性
本研究では、新規性を二点に絞る。
4.1 Tunnelを共通制御リソースとする抽象化
第一の提案は、ネットワーク機器そのものではなく、拠点間に形成されるTunnelを共通制御リソースとして扱うことである。
Ibukiの上位制御層は、strongSwanなど個別実装の設定方法や内部状態を直接扱わない。各実装に対応するAdapterが、Ibukiの共通モデルと個別実装との間を変換する。
一方ですべてのTunnel実装が同一の機能を提供するとは限らない。そのため、各Adapterは利用可能な機能をCapabilityとして公開する。
例えば、ある実装ではTunnelの確立、削除、状態取得、Immediate Transitionには対応できるが、Flow Preserveには対応できない可能性がある。
この場合Ibukiは、機器差を無理に隠すのではなく、Capabilityの差として上位層へ公開し、その範囲内で制御計画を生成する。
したがって、本研究における「機器依存性の低減」とは、すべての機器を完全に同じ方法で扱うことではない。
機器固有の差異をAdapterへ局所化し、Path SelectionやPath Transitionなどの中核ロジックを特定の製品や実装から分離することを意味する。
4.2 Path Transitionを第一級の制御対象とする
第二の提案は、Path変更を単なる設定変更としてではなく、独立した状態遷移として扱うことである。
Ibukiでは、Path Transitionを少なくとも、Proposed、Preparing、Validating、Ready、Committing、Verifying、Completed、Rolling Back、Failedといった状態として管理する。
これにより、例えば「最終的にはDirectを利用したいが、現在はDirect Tunnelを検証中であり、通信はHubを利用している」という状態を明確に表現できる。
また、Transitionそのものの状態機械と、通信をどのように移すかというStrategyを分離する。
初期的にはImmediate、Graceful、Flow Preserveの三方式を想定する。
Immediateは切替速度を優先し、Forwardingを直ちに新Pathへ変更する。Gracefulは、旧Pathを短時間Drainした後に新Pathへ移行し、順序入れ替わりの抑制を重視する。Flow Preserveは、既存フローを旧Pathに残し、新規フローから新Pathを利用することで、通信継続性を優先する。
これにより、「どのPathを利用するか」と「そのPathへどのように移行するか」を独立して変更・比較できる。
4.3 二つの新規性の関係
Tunnelの抽象化とPath Transitionの状態モデルは相互に依存する。
Tunnelを抽象化するだけでは、Ibukiは共通設定APIに留まる。一方でTransition Modelが存在しても、それがstrongSwanなど特定実装に固定されていれば、汎用的な研究基盤にはならない。
そのため本研究では、Tunnel Resource AbstractionとExplicit Path Transition Modelの組み合わせを研究の中心に据える。
管理ドメイン間協調、Explain機能、GUI、Health Evaluationなどは、この二つの研究課題を支える機能として位置付ける。

5. Research Question
本研究では、以下のResearch Questionを設定する。
RQ1: Tunnelの共通抽象化によって、異なるTunnel Backend間でPath SelectionおよびPath Transitionの上位ロジックを再利用できるか。
RQ2: Prepare、Validate、Commit、Verify、Rollbackを明示したTransition Modelによって、単純な経路更新と比較して安全なPath変更を実現できるか。
さらに補助的な課題として、以下を扱う。
RQ3: 複数の管理Domainに属するController間でも、同じTransition Modelを利用してPath変更を協調実行できるか。
RQ4: Path SelectionおよびTransitionの判断過程をDecision Logとして保存することで、経路変更の理由を再現可能に説明できるか。
RQ3およびRQ4は重要な機能ではあるが、本研究の中心的な新規性とは位置付けない。

6. 提案システム
Ibukiは、Intent API、Policy Engine、Path Selection Engine、Transition Engine、State Manager、Agent、Tunnel Adapter、Forwarding Adapter、Health Probe、Explain/Event Store、Federation Interfaceから構成される。
利用者は、個々のstrongSwanやVPPの設定を直接指定するのではなく、どの拠点間を接続したいか、どのPathを利用可能とするか、どの品質条件を満たす必要があるか、どのTransition Strategyを利用するかといった要求をIntentとして指定する。
Policy EngineはIntentを解釈し、Path Selection Engineが利用可能な候補Pathを評価する。Target Pathが決定すると、Transition EngineがPathからTarget Pathへの状態遷移を管理する。
実際のOSS操作は各拠点のAgentが担当する。AgentはTunnel AdapterやForwarding Adapterを介してstrongSwanやVPPを操作し、Observed StateをControllerへ報告する。
具体的なController、Agent、Adapter、通信Protocolの実装については、初期実装完了後に実装章へ追記する。

7. リソースモデル
7.1 Node
Nodeは、Tunnelの終端またはPath上のWaypointとなる論理的なネットワーク地点を表す。
NodeにはNode ID、所属Domain、Tunnel Endpoint、Capability、Administrative State、Observed Stateなどの情報を保持する。
Nodeは物理ルータと必ずしも一対一で対応する必要はなく、仮想マシン、Linux Network Namespace、クラウド上のVPN GatewayなどもNodeとして扱える。
7.2 Tunnel
Tunnelは二つのNode間に形成される論理的な接続を表す。
初期研究ではIPsec Tunnelを主な対象とする。
Tunnelには、Tunnel ID、Local Node、Remote Node、Protocol、Desired State、Applied State、Observed State、Health State、Capabilityなどがある。
7.3 Segment
SegmentはPathを構成する一区間を表す。
例えばSite AからRelay Cを経由してSite Bへ到達するPathでは、Site AとRelay Cの間、Relay CとSite Bの間という二つのSegmentが存在する。
各Segmentは原則として一つのTunnelに対応する。
7.4 Path
Pathは、一つ以上のSegmentによって構成されるEnd-to-Endの通信経路である。
Ibukiでは、Direct、Hub、Relayを別のPath種別として特別扱いしない。
Direct経路はWaypointを持たないPathとして表し、HubやRelayを経由する場合は一つ以上のWaypointを持つPathとして表現する。
このモデルによって、経由地点の種類にかかわらず、同一のPath SelectionおよびPath Transitionを適用できる。
7.5 Candidate Path
初期段階では、Controllerがネットワーク内のNodeを自由に組み合わせ、任意のPathを自動生成することは想定しない。
管理者または上位システムによって許可されたCandidate Path集合の中から選択する。
これは、意図しないTransit利用、Loop、セキュリティ境界違反、Tunnel数の爆発、Path探索空間の過剰な増大を防ぐためである。

8. 状態モデル
Ibukiでは、Desired State、Applied State、Observed State、Health State、Transition Stateを区別する。
Desired Stateは、利用者またはPolicyが要求する目標状態である。
Applied Stateは、Ibukiが下位のOSSへ適用したと認識している状態である。
Observed Stateは、strongSwanやVPPなどから実際に観測された状態である。
Applied StateとObserved Stateは必ずしも一致しない。例えば、IbukiがDirect向けのForwardingを設定したと記録していても、実際の通信が旧Pathを利用し続けている可能性がある。
この差異を検出できることは、Ibukiの状態管理において重要である。
Health Stateは、TunnelまたはPathが正常に利用可能かを示す。Unknown、Healthy、Degraded、Unhealthy、Failedなどの状態を想定する。
Transition Stateは、Path変更処理がどの段階にあるかを示す。
最終的なTarget Pathだけでなく、「現在はCandidate Tunnelを準備している」「End-to-End検証を行っている」「切替後検証中である」といった中間状態を管理する。

9. Intent
利用者は、strongSwanやVPPの具体的なコマンドではなく、達成したいネットワーク状態をIntentとして指定する。
Intentには、通信元、通信先、利用可能なCandidate Path、必須または禁止するWaypoint、Path Priority、許容RTT、許容Packet Loss、Transition Strategy、Fallback Path、Timeout、Verification Conditionなどを含める。
GUIは、このIntentを入力するためのインターフェースの一つとして位置付ける。
Ibuki内部では、GUI、API、構成ファイルなどから得られた情報を共通Intent Schemaへ変換する。
具体的なSchema形式は、実装完了後に実装章で記載する。

10. Path Selection
Path Selectionでは、まずIntentによって許可されているCandidate Path集合を取得する。
次に、利用不能なPathやConstraintに違反するPathを候補から除外する。
その後、残ったPathについて比較規則を適用し、Target Pathを決定する。
初期的にはExplicit、Priority、Evaluatedの三方式を想定する。
Explicitは管理者がPathを直接指定する方式である。
Priorityはあらかじめ定義されたPathの優先順位に基づいて選択する。
Evaluatedは、RTTやPacket Lossなどの観測値を比較して選択する。
Evaluated Modeでは重み付きScoreを用いることも可能であるが、重みや正規化方法によって判断理由が分かりにくくなる場合がある。
そのため、初期的にはPacket Loss、RTT、Administrative Priority、Path IDといった複数条件を順番に比較する辞書式評価を基本とする。
また、観測値のわずかな変化によってPathが頻繁に切り替わることを防ぐため、Hysteresis、Confirmation Count、Minimum Active Time、Hold-downなどの安定化機構を利用する。

11. Path Transition
Path Transitionは、Target Pathが決定した後に実行される。
まずPrepare段階で、Target Pathに必要なTunnelおよびForwarding Stateを準備する。この段階ではCurrent Pathを変更しない。
次にValidate段階で、Candidate PathがEnd-to-Endで利用可能であることを確認する。単にIPsec SAが存在しているだけではなく、必要に応じてReachability、RTT、Packet Loss、Waypoint通過条件なども検証する。
必要なNodeがすべて切替可能な状態になればReadyとなる。
その後Commitを実行し、実際のForwardingをTarget Pathへ変更する。
Commit後はVerify段階へ移行し、新しいPathで通信が正常に成立しているかを確認する。
検証に成功した場合、TransitionはCompletedとなる。旧PathはPolicyに応じてStandby、Drain、または削除可能状態となる。
Prepare以降の処理で失敗した場合にはRollbackを実行する。
Rollbackでは可能な限りTransition開始前に利用していたPathへ戻す。
このRollbackを可能にするため、新Pathへ切り替えた直後に旧Pathを完全に削除するのではなく、一定期間復旧可能な状態として保持する設計を想定する。

12. 管理ドメイン間協調
Ibukiでは、ネットワーク全体を一台の中央Controllerが管理することを必須としない。
組織や管理主体、セキュリティ境界ごとに複数のDomain Controllerを配置できる構成とする。
各Controllerは、自分が管理するDomainに属するAgentやTunnelのみを直接操作する。
異なるDomainをまたぐTunnelを構築する場合、一方のControllerが他方のAgentへ直接設定を送信することはしない。
代わりに、Controller間で高レベルなTransition Proposalを交換する。
例えば、Controller AがController Bへ、「Site AとSite Bの間にIPsec Direct Pathを構築し、Graceful Transitionを行うことが可能か」という要求を送る。
Controller Bは、自DomainのPolicyやCapabilityを確認したうえで、その要求をAcceptまたはRejectする。
双方が参加可能であれば、各Controllerは自Domain内のAgentへPrepareを指示する。
両DomainがReadyとなった時点でCommitを調整し、それぞれのDomainでLocal Commitを実行する。
切替後には各DomainでVerificationを実施し、双方が正常であることを確認した時点でTransitionを完了する。
Controller間のControl Plane通信は、Ibukiが切替対象として管理するData Tunnelには依存させない。
これは、Data Tunnelの障害によってController間通信まで失われ、Tunnel復旧処理そのものが実行できなくなる循環依存を防ぐためである。
初期実装では、相互TLS認証を用いたHTTPSまたはgRPC等のControl Channelを利用することを想定する。
具体的なFederation ProtocolおよびAuthentication方式は、実装章で記載する。

13. Explainability
Ibukiでは、Path SelectionおよびPath Transitionで行われた判断をDecision Logとして保存する。
例えばDirect PathがPacket Loss制約に違反したため除外され、Relay PathとHub Pathが候補として残り、その中でRelay PathのRTTが小さかったため選択された、という判断を記録する。
Transitionについても、利用したStrategy、Prepareの結果、Validation結果、Commit結果、Verification結果、Rollbackの有無などを保存する。
Explain機能は、自然言語生成自体を研究対象とするものではない。
システムが内部で実際に行った判断を、運用者が追跡可能な形式で提示することを目的とする。
これにより、動的に経路が変更された後でも、「なぜ現在このPathを利用しているのか」を確認できるようにする。

14. 既存技術との比較
14.1 ONOS
ONOSはSDN ControllerとしてIntent Frameworkを提供している。
利用者やアプリケーションは具体的なForwarding Ruleそのものではなく、ネットワークに対するPolicyや接続要求をIntentとして表現できる。
ONOSはIntentを解釈し、Flow RuleやTunnel Linkなどのネットワーク操作へ変換する。
この点はIbukiと共通する。
したがって、Intentを用いることやネットワーク状態を抽象化することそのものは、Ibukiの新規性ではない。
一方、Ibukiでは制御対象をTunnelとPathへ限定する。また、Target Pathの決定後に発生するPrepare、Validate、Commit、Verify、Rollbackといった通信への影響を伴う遷移を、共通モデルとして扱うことを研究対象とする。
14.2 Kubernetes
Kubernetesは、宣言されたDesired Stateと実際の状態を比較し、Controller Loopによって現在状態を目標状態へ近づける。
IbukiにおけるDesired State、Observed State、Reconciliationという考え方は、この設計思想と類似する。
したがって、宣言的状態管理そのものもIbukiの新規性ではない。
一方で、ネットワークPathの変更では、最終的な状態だけでなく途中の操作順序が通信へ直接影響する。
最終的にDirect Pathを利用できればよいのではなく、それまでの間Current Pathをどの段階まで維持するか、どの段階で切り替えるか、切替失敗時にどこへ戻るかを制御する必要がある。
Ibukiでは、この通信固有の状態遷移をPath Transitionとして独立して扱う。
14.3 flexiWAN
flexiWANはオープンソースのSD-WAN基盤であり、中央管理システム、Edge、Agent、VPP、FRRなどを組み合わせた実運用可能なSD-WANシステムを提供している。
また、設定の適用順序管理、状態復元、トランザクション処理、失敗時Rollbackなども実装されている。
したがって、ControllerとAgentを配置すること、VPPやFRRを利用すること、OSSによるSD-WANを構築すること、設定をRollbackすることは、Ibukiの新規性とはならない。
flexiWANがSD-WAN製品として実際のネットワークを構築・運用することを目的とするのに対し、Ibukiでは異なるTunnel Backendに対して共通のPath Transition Modelを適用し、Path SelectionとPath Transitionを独立に交換・評価できる研究基盤を目指す。

15. 本研究の位置付け
Ibukiは、ONOSのIntent-based Networking、Kubernetesの宣言的状態管理、flexiWANのOSS SD-WAN実装と共通する要素を持つ。
そのため、本研究では「従来存在しなかったネットワークControllerを新たに作る」という主張は行わない。
本研究が検証するのは、既存のTunnelを限定された共通制御境界として利用し、実装固有処理をAdapterへ分離することで、異なるVPN実装に対して同じ上位制御ロジックを適用できるかという点である。
さらに、通信Pathの変更を明示的なTransitionとして扱うことで、異なるTransition Strategyを同一の状態モデル上で比較・利用できるかを検証する。
Ibukiの研究上の価値は、この二つのモデルが実際のネットワーク制御において成立するかを検証することにある。

16. 評価方針
評価では、Ibukiが単に動作することを確認するだけでは不十分である。
本研究で設定した二つの新規性について、それぞれ有効性を検証する。
一つ目は、Tunnel抽象化による実装依存性の低減である。
異なるTunnel Backendを利用した場合に、Path Selection EngineやTransition Engineを変更せず、同じIntentおよびTransition Scenarioを実行できるかを評価する。
二つ目は、明示的なPath Transition Modelの有効性である。
単純な経路更新方式と比較し、Prepare、Validate、Commit、Verify、Rollbackを用いることで、通信断時間、障害復旧時間、状態追跡可能性がどのように変化するかを評価する。

17. 評価環境
評価用Testbedでは、最低限Site A、Site B、Hub、Relay、Controller、Measurement Nodeを用意する。
Site AとSite Bの間には、Direct Path、Hub経由Path、Relay経由Pathの三種類を構築する。
これらは実験上Direct、Hub、Relayと呼ぶが、Ibuki内部ではすべて同一のPath Resourceとして扱う。
Data PlaneにはIPsecを利用し、初期BackendにはstrongSwanを想定する。
Forwarding PlaneにはVPPまたはLinux Forwardingを利用する。
ControllerとAgent、Controller同士のControl Planeは、Data Planeとは独立したManagement Network上へ配置する。
これにより、Data Tunnelの障害とControl Planeの障害を独立して発生させることができる。
また、Network Emulationを利用して、各Pathに遅延、Jitter、Packet Loss、一時的なLink Downなどを人工的に発生させる。
Linuxのtc netem等を利用することを想定しているが、具体的な実装方法およびParameterは実装完了後に追記する。

18. 評価実験
18.1 基本的なPath Transition
Hub PathからDirect PathへのTransitionを実行し、各Transition Phaseが期待通りに進行することを確認する。
測定対象として、Prepare開始時刻、Tunnel Ready時刻、Validation完了時刻、Commit時刻、Forwarding変更時刻、Verification完了時刻、Transition完了時刻を記録する。
これにより、Transition全体の時間だけでなく、各段階に要した時間を分離して評価する。
18.2 ImmediateとGracefulの比較
同一のCurrent PathとTarget Pathについて、ImmediateとGracefulを複数回実行する。
切替所要時間、Packet Loss、最大連続通信断時間、Packet Reordering、TCP Retransmissionなどを測定する。
これにより、切替速度と通信への影響とのTrade-offを定量的に比較する。
18.3 Flow Preserveの評価
Flow Preserveが初期実装に含まれる場合には、長時間継続するTCP接続を維持した状態でTransitionを実行する。
既存Flowが旧Pathを利用し続け、新規FlowがTarget Pathへ割り当てられることを確認する。
既存Sessionの継続率、新規Flowの移行時間、旧Pathを維持する必要がある時間、Flow State管理量などを評価する。
初期実装に含まれない場合は、この評価を将来課題とする。
18.4 Prepare失敗
Candidate Tunnelを確立できない状態を人工的に発生させる。
このとき、Current Pathを維持したままTransitionを中止できることを確認する。
本実験では、失敗そのものを防ぐことではなく、失敗した場合に通信への影響を抑えられるかを評価する。
18.5 Verification失敗とRollback
Commit後にTarget Pathへ障害を発生させる。
IbukiがVerification Failureを検出し、以前のPathへRollbackできるかを確認する。
障害検出時間、Rollback開始時間、Previous Path復旧時間、最大通信断時間、Rollback成功率を測定する。
18.6 Control Plane障害
Data Planeが正常な状態で、ControllerとAgent間、またはController同士のControl Channelを遮断する。
期待する挙動は、Control Planeが停止しても現在のActive Pathが維持されることである。
Control Plane復旧後にはObserved Stateを再取得し、Controllerが現在状態を再認識できることを確認する。
18.7 Tunnel Backend交換
同一のIntent、同一のPath Model、同一のTransition Scenarioを、異なるTunnel Adapterで実行する。
このとき、Path Selection EngineおよびTransition Engineへ必要となった変更量を測定する。
また、共通Test Caseをどの程度再利用できたか、Backend固有処理がどの程度Adapter内部へ局所化されたかを評価する。
18.8 管理ドメイン間Transition
Site AとSite Bを異なるDomain Controller配下へ配置する。
一方のControllerが相手DomainのAgentを直接操作せず、Proposal、Acceptance、Prepare、Ready、Commit、Verification、Confirmationを経てPath Transitionできることを確認する。
さらに、片側Prepare失敗、Commit前のControl Channel切断、Commit後のControl Channel切断などを発生させ、失敗時の動作を評価する。

19. 評価指標
Transition性能については、Transition全体の所要時間に加え、Preparation Time、Validation Time、Commit Time、Verification Time、Rollback Timeを測定する。
Data Planeへの影響については、Packet Loss、Packet Reordering、Maximum Outage Duration、TCP Retransmission、Session Continuityを評価する。
障害復旧については、Failure Detection Time、Rollback Start Time、Recovery Time、Rollback Success Rateを測定する。
Control Planeの負荷として、TransitionあたりのMessage数、Controller CPU使用率、Agent CPU使用率、Memory使用率、Event処理数などを記録する。
Tunnel抽象化の有効性については、Backendを変更した際の共通コード率、Adapter固有コード量、上位Engineへの変更量、共通Test Suiteの再利用率を評価する。

20. 実装
本章は、初期実装完了後に追記する。
最終稿では、使用したプログラミング言語、Runtime、ControllerとAgentのProcess構成、Intent Schema、Adapter API、Capability Schema、State Store、strongSwan VICIとの連携方法、Forwarding Adapter、Controller-Agent間Protocol、Controller間Federation Protocol、認証方式、Explain/Event Storeの実装について記述する。
また、使用したOS、Kernel、strongSwan、VPP、FRR等のVersionを明記し、実験の再現性を確保する。

21. 実験結果
本章では、現時点の初期実装およびLinux VM上の検証環境で確認できている結果を整理する。
ただし、本稿執筆時点では、すべての評価実験について統計的な反復測定が完了しているわけではない。
そのため、本章ではまず、提案モデルが実装可能であること、主要な状態遷移が実行可能であること、既存OSSを組み合わせた実ネットワーク上で疎通確認ができていることを中心に記述する。
切替時間、Packet Loss、Packet Reordering、CPU使用率などの定量値は、今後の反復実験によって追加する。

21.1 実装済みPrototypeの範囲
現時点のPrototypeでは、Controller coreをC言語で実装している。
ControllerはYAMLで記述されたNode、Tunnel、Segment、Path、Intent、Path Selection Policyを読み込み、Intentに対して利用すべきPathを選択する。

実装済みの主な要素は次のとおりである。

YAMLによるIntent、Path、Tunnel、VPP edgeの記述
Priorityに基づくPath Selection
Active Path障害イベントに基づくFallback Selection
RTTおよびPacket Lossを用いたEvaluated Selection
選択理由と除外理由のExplain出力
strongSwan向けswanctl設定生成
VPP向けroute plan生成
Linux network namespaceを用いた複数拠点Testbed
Direct IPsec smoke test
Hub経由IPsec smoke test
VPP host-interface forwarding smoke test
Controller生成planによるIPsecおよびVPP統合runtime smoke test
Scenario harnessによる複数条件のPath Selection検証

このPrototypeは、完成した商用ネットワークControllerではない。
しかし、Ibukiの中心的な仮説である「標準TunnelをPath Resourceとして抽象化し、Path SelectionとPath Transitionを上位ロジックとして扱えるか」を検証するための最小実装として機能している。

21.2 評価環境の構成
評価環境は、一台のLinux VM上に複数のnetwork namespaceを作成する構成とした。
namespaceは、少なくとも次の論理Nodeを表す。

site-a
site-b
hub-1
relay-c

site-aとsite-bの間には、次の三種類のPathを定義した。

Direct Path:
site-aからsite-bへ直接到達するPath

Hub Path:
site-aからhub-1を経由してsite-bへ到達するPath

Relay Path:
site-aからrelay-cを経由してsite-bへ到達するPath

これらは実験上Direct、Hub、Relayと呼ぶが、Controller内部ではいずれもPath Resourceとして扱う。
Directを特別な型として扱うのではなく、Waypointを持たないPathとして表現する。
HubおよびRelayはWaypointを持つPathとして表現する。

この構成により、物理的な複数拠点を用意せずに、複数Path、Fallback、Relay選択、VPP forwarding、IPsec tunnel確立を一台のVMで再現できた。

21.3 YAML IntentからPath Selectionまでの確認
YAMLで定義したIntentに対して、ControllerがPath候補を読み取り、Policyに基づいてPathを選択できることを確認した。

Priority Selectionでは、Direct Pathが利用可能な場合にDirect Pathが選択された。
Active PathとしてDirect Pathを指定し、そのDirect Pathに障害イベントを注入した場合、Fallback候補としてHub Pathが選択された。
Evaluated Selectionでは、RTTおよびPacket Lossの注入値に基づき、Direct、Hub、Relayの中から条件に合うPathを選択できることを確認した。

この結果から、Path Selection Engineは、少なくとも次の三種類の選択を同一のIntent/Path Model上で扱えることを確認した。

Priority-based selection
Failure-triggered fallback selection
Metric-evaluated selection

また、選択結果にはselected_pathだけでなく、reasonおよびexcluded pathが出力される。
これにより、Path選択の理由を後から確認できる。

21.4 Scenario Harnessによる状態遷移確認
本番daemon化の前段として、Scenario Harnessを実装した。
Scenario Harnessでは、CLI引数または定義済みstepにより、Path障害、回復、品質条件変化を注入できる。

確認済みのscenarioは次のとおりである。

PriorityによりDirect Pathを選択するscenario
Direct Path障害によりHub PathへFallbackするscenario
Direct Path障害後、Direct回復により再びDirectへ戻るscenario
品質条件によりRelay Pathを選択するscenario
Direct障害からruntime planを生成するscenario
Direct、Fallback、Recovery、Relay-bestを連続実行するmulti-step scenario

これらのscenarioにより、Path Selectionだけでなく、Transition前後でControllerがどのPathを選ぶべきかを実験的に確認できた。

現段階のScenario Harnessは本番daemonではない。
しかし、eventnetdまたはIbuki Controller daemonが将来扱うべきイベント列を先に固定する役割を持つ。
これにより、daemon化前にPath SelectionおよびTransition判断の仕様を検証できる。

21.5 Direct IPsec Pathの確認結果
Direct Pathについては、site-aとsite-bのnamespace内でstrongSwan charonを起動し、swanctlを用いてIPsec connectionおよびcredentialをloadした。
その後、site-aからsite-bへprotected LAN間のpingを実行し、疎通が成功することを確認した。

さらに、swanctlのSA情報からESP packet counterが増加することを確認した。
これにより、単なるunderlay疎通ではなく、IPsec SA上を実際にpacketが通過していることを確認できた。

Direct IPsec smoke testで確認した内容は次のとおりである。

site-a/site-b namespace内でcharonを起動できること
VICI socket経由でswanctl操作ができること
swanctl.confをloadできること
Direct CHILD SAをinitiateできること
site-aからsite-bへのprotected LAN pingが成功すること
ESP packet counterが増加すること

この結果により、Ibukiが制御対象とする標準IPsec Tunnelを、Linux namespace環境上で実際に生成・利用できることを確認した。

21.6 Hub IPsec Pathの確認結果
Hub Pathについては、site-aからhub-1、hub-1からsite-bの二つのIPsec区間を構成した。
Hub Pathは複数Segmentから構成されるため、Direct PathよりもPath Resourceとしての抽象化が重要になる。

Hub Pathでは、route-based IPsecを想定し、XFRM interfaceを用いて各Segmentを接続する構成を検証した。
site-a、hub-1、site-bの各namespaceで必要なcharonを起動し、二つのTunnelを確立したうえで、site-aからsite-bへのLAN疎通を確認した。

Hub IPsec smoke testで確認した内容は次のとおりである。

site-a-hub間Tunnelを確立できること
hub-site-b間Tunnelを確立できること
hub-1でforwardingを有効化できること
XFRM interfaceを用いて経路を接続できること
site-aからsite-bへのpingがHub経由で成功すること
各SegmentのESP counterが増加すること

この結果により、単一Tunnelだけでなく、複数Tunnelから構成されるPathもIbukiのPath Modelで扱える見通しを得た。

21.7 VPP Forwardingの確認結果
VPPについては、Linux namespaceとVPPをvethおよびAF_PACKET host-interfaceで接続し、site-aとsite-bのLAN trafficをVPP経由で転送できることを確認した。

VPP netns smoke testでは、site-aおよびsite-bのnamespace側interfaceと、VPP側host-interfaceを対応付けた。
その上で、VPP FIBに必要なrouteを投入し、site-aからsite-bへのLAN疎通が成立することを確認した。

また、ControllerがYAMLのvpp_edges定義からVPP route planを生成し、そのplanを実VPPに適用した場合にもLAN疎通が成功することを確認した。

この結果により、Ibukiが生成したForwarding planを、実際のVPP forwarding planeへ反映できることを確認した。

21.8 IPsecとVPPの統合runtime確認
Controllerが選択したPathから、IPsec runtimeとVPP forwarding runtimeを一つの生成planとして扱う統合smoke testを実行した。

このtestでは、ControllerがYAML Intentを読み込み、Pathを選択し、選択結果に応じて次の操作を連続して実行した。

IPsec runtimeの起動
IPsec smokeによるESP counter確認
VPP host-interface setup
YAML vpp_edgesから生成したVPP route planの適用
VPP forwarding smoke

Direct modeでは、Direct Pathに対応するIPsecおよびVPP forwardingを制御し、疎通が成功することを確認した。
Fallback modeでは、Direct Path障害イベントを注入し、Hub Pathを選択したうえで統合runtime smokeが成功することを確認した。

2026年8月12日に実行した自動評価では、`scripts/vm-evaluate.sh` によりDirect modeおよびFallback modeの統合runtimeを連続して確認した。
この評価では、`samples/linux-vm-netns.yaml` を入力とし、Controllerが生成したplanに基づいてIPsec runtimeとVPP forwardingを適用した。
結果として、integrated-directは51,944 msでpassし、integrated-fallbackは75,599 msでpassした。
ここでの時間は、Path選択そのものの時間ではなく、IPsec起動、SA確立、VPP host-interface設定、route投入、疎通確認を含むend-to-end smoke test全体の実行時間である。
したがって、この値をそのまま実運用時の切替停止時間とみなすことはできない。
一方で、Controllerが生成した単一のruntime planから、strongSwanとVPPの両方を制御し、Direct PathおよびHub Fallback Pathの双方で実trafficを通せたことは確認できた。

この結果から、IbukiのPrototypeは、Path Selectionの結果を単なる表示に留めず、strongSwanおよびVPPへ反映するruntime planへ接続できている。

21.9 Explain出力の確認結果
Scenario Harnessでは、Path選択結果をExplain JSONLとして出力できる。
Explainには、少なくとも次の情報を含める。

Intent ID
selected_path
transition_state
reason
excluded path
注入されたhealth情報
期待結果
実行結果

これにより、Pathが選択された理由だけでなく、候補から除外されたPathとその理由を確認できる。

例えばDirect Path障害時には、Direct Pathがactive path failed eventにより除外され、FallbackとしてHub Pathが選択されたことを説明できる。
Evaluated Selectionでは、Packet LossやLatencyの比較順序に基づいてRelay Pathが選択されたことを説明できる。

この結果は、RQ4で述べた説明可能性に対する初期的な実装結果である。

2026年8月12日の評価では、Explain JSONL確認は527 msでpassした。
この確認では、Direct正常、Direct障害、Direct回復、Relay-bestというmulti-step scenarioを実行し、Explain JSONL内に`path-direct`、`path-via-hub`、`path-via-relay-c`の各選択結果と`result: pass`が記録されることを確認した。
これにより、少なくともScenario Harness上では、Controllerの判断過程を事後的に再生可能な形で保存できている。

21.10 研究課題に対する現時点の結果
RQ1について、現時点ではstrongSwanを中心とするBackendで検証している。
異なるTunnel Backend間での完全な比較は未完了である。
しかし、Controller coreがPath、Segment、Tunnel、Capability、Adapterという境界を持つ構造になっており、上位のPath SelectionロジックがstrongSwan固有の設定文字列を直接扱わない形になっている。
このため、Tunnel抽象化の実装可能性は確認できたが、Backend交換性の定量評価は今後の課題である。
また、同じYAMLおよび選択coreからswanctl向け計画とVPP/netns向け計画を生成するbackend-reuse確認は910 msでpartialとなった。
これは、上位Engineの再利用可能性を初期的に示す一方で、Backend plugin interfaceとして完成しているわけではないためである。

RQ2について、DirectからHubへのFallback、HubからDirectへのRecovery、Direct/Hubのruntime生成、IPsec/VPP統合smokeにより、Path変更を中間状態を持つ制御手順として扱える見通しを得た。
ただし、Immediate、Graceful、Flow Preserveを定量比較する実験は未完了である。
Direct障害からFallback runtime plan生成までの確認では、5回反復の平均plan生成時間が569 msであった。
この値はController側のplan生成確認であり、IPsec再確立やVPP適用を含む実際のtraffic切替完了時間ではない。
一方、統合runtime smokeではDirect modeが51,944 ms、Fallback modeが75,599 msでpassしており、現在のPrototypeが少なくとも実network namespace環境上でend-to-end制御を完了できることを確認した。

RQ3について、複数Domain Controller間の協調Protocolは設計段階であり、実装評価は未完了である。
2026年8月12日の評価でも、federationはskipとして記録している。
これは、Transition Proposalや二相Commit型のDomain間協調が設計対象として残っているためである。

RQ4について、Scenario HarnessのExplain JSONLにより、Path選択理由と除外理由を記録できることを確認した。
ただし、本番運用に耐えるEvent Store、Query API、GUIでの可視化は今後の課題である。

21.11 未測定項目
現時点で未測定または今後追加すべき項目は次のとおりである。

Transition全体の所要時間
Preparation Time
Validation Time
Commit Time
Verification Time
Rollback Time
Packet Loss
Packet Reordering
Maximum Outage Duration
TCP Retransmission
Session Continuity
Controller CPU使用率
Agent CPU使用率
Memory使用率
TransitionあたりのControl Plane Message数
Backend交換時の上位Engine変更量
共通Test Suite再利用率

これらは、実験環境を固定し、複数回反復実験を行ったうえで、平均値、中央値、最大値、標準偏差などを用いて評価する。

21.12 自動評価Scriptによる確認結果
以下は2026年8月12日時点の初回評価結果であり、現在の実装状態を示すものではない。後続の修正・追加実装は21.13に反映する。
今後の評価項目を継続的に確認するため、`scripts/vm-evaluate.sh`を追加した。
このScriptは、Scenario Harness、fallback plan生成、Explain JSONL、Backend reuse、統合runtime smokeをまとめて実行し、結果をMarkdownおよびCSVとして`out/evaluation/`以下に保存する。
また、現時点で未実装の項目については、成功扱いにせず、partialまたはskipとして明示的に記録する。

2026年8月12日に、次のコマンドで評価を実行した。

`sudo RUN_RUNTIME=1 sh scripts/vm-evaluate.sh all samples/linux-vm-netns.yaml`

結果は`out/evaluation/20260812-124723/summary.md`および`out/evaluation/20260812-124723/summary.csv`に保存された。
確認結果の概要は次のとおりである。

scenarioは1,673 msでpassした。
この項目では、Direct選択、Direct障害によるHub Fallback、評価値に基づくRelay選択、multi-step scenarioを確認した。

fallback-timeは3,297 msでpassした。
5回反復したFallback runtime plan生成の平均時間は569 msであった。
これはControllerが障害イベントを受けてFallback Pathを選び、runtime planを生成するまでの時間である。

explainは527 msでpassした。
Explain JSONLに、選択されたPath、判断理由、除外理由、期待結果、実行結果が記録されることを確認した。

transition-policyはpartialであった。
Immediate相当のFallback判断および統合runtimeは確認済みである一方、Graceful TransitionおよびFlow Preserve Transitionは未実装である。

rollbackはpartialであった。
Fallback plan生成は確認できたが、切替失敗時に旧Pathへ戻すruntime rollback executorは未実装である。

backend-reuseはpartialであった。
同じYAMLおよびPath Selection coreから、swanctl向け計画とVPP/netns向け計画を生成できることは確認した。
ただし、Backendを差し替えるための正式なplugin interfaceや共通test suiteは未完成である。

control-planeはskipであった。
現時点のPrototypeは生成plan実行型であり、eventnetd daemon停止時にもActive Pathを維持できるかを検証するtestは未実装である。

restart-recoveryはskipであった。
Observed StateからController再起動後に状態を復元する仕組みは未実装である。

federationはskipであった。
複数Domain Controller間のTransition Proposal protocolは設計対象であり、Prototypeにはまだ含まれていない。

integrated-directは51,944 msでpassした。
Controllerが選択したDirect Pathについて、IPsec runtime起動、ESP counter確認、VPP forwarding設定、LAN疎通確認までを完了できた。

integrated-fallbackは75,599 msでpassした。
Direct Path障害イベントを注入した場合にHub Pathを選択し、IPsecとVPP forwardingを組み合わせたruntimeでLAN trafficを疎通できた。

以上の結果から、現時点のPrototypeは、設計全体のうちPath抽象化、Path Selection、Explain、strongSwan/VPPへのruntime反映、DirectおよびFallbackの統合smokeまでを実装・検証できている。
一方で、Graceful/Flow Preserve、Rollback executor、daemon化、Observed State recovery、Multi-domain federationは未実装であり、今後の実装対象として残っている。

21.13 現在の実装状態への更新
前項の評価は2026年8月12日時点の記録である。その後、評価結果を再現可能にするため、eventnetdの実装と評価Scriptを拡張した。

AgentはPathごとのRTT、Packet Loss、Jitter、連続失敗回数および連続成功回数をJSONL telemetryとして出力できる。eventnetdはこのtelemetryを周期的に読み込み、batch単位で複数Pathの観測値を反映してからPathを再選択する。Path FailureおよびRecovery event、strongSwanのCHILD SA観測、VPP FIB観測も共通のObserved State入力へ変換できる。
現在の実装では、Gracefulのdraining・cleanup・Post Validation、Rollback plan/runtime cleanup、eventnetdの周期・socket・reload・安全停止、state fileからのActive Path復元、VLAN/VRF/FIB評価も追加されている。VICIは操作・観測・event購読の接合まで、VPP Binary APIは低レベルtransportと生成message境界まで実装済みである。一方、Flow Preserve、VPPの版依存codecによる実FIB操作、VLAN対象外dropの実パケット検証、FRR/BGP連携、HAおよびMulti-domain federationは未実装または未検証である。

Controllerの判断結果は、`ibuki.status.v1`のstatus JSONLとして保存できる。Intent ID、Path ID、ReasonはJSON文字列としてエスケープされ、入力由来の引用符や制御文字によって記録形式が破壊されない。YAML読み込み時には、識別子、route属性、重複route、重複waypoint、required/forbidden waypointの矛盾も検証する。

eventnetdは、Linux Unix socketからAgentのJSONLを受信できる。複数接続はstate fileを介して逐次処理し、`--socket-accept-count 0`では無期限に接続を受け付ける。SIGHUPによる設定再読み込み、不正設定時の旧設定維持、SIGTERM/SIGINTによる安全停止も実装した。これは並列Agentを一つのメモリ上Controllerで処理する実装ではなく、競合を避けた逐次接続モデルである。

YAML route表現については、旧式単一路由、明示route、双方向route、node別route、table/metric/interface、Relay直列、非対称route、予備Path、VLAN Policyをサンプルとsmokeで確認できる。VLAN IDはPath SelectionとVPP sub-interface生成へ反映されるが、VLANタグ付き通信を実VPPで継続的に運用する部分は環境依存の検証課題として残る。

strongSwanについては、PSKおよび証明書認証の`swanctl.conf`生成、証明書ファイルのreadability、有効期限の事前確認、VICI socket指定、CHILD SA観測を実装した。libviciを用いた操作・観測・有限／無期限の`child-updown` event購読と、eventnetdからの再reconcile接合までを実装している。rekey／DPD等のイベント別ポリシーとservice再起動時の購読復旧固定は未実装である。VPPはvppctlによるruntime反映とFIB/interface検証に加え、Binary APIの接続・受信FD・dispatch・generic callback・生成messageの所有権境界まで実装した。VAPI生成コードによるroute／VLAN／VRFの具体codecと実FIB操作は、対象SDK版を固定した後段へ残る。

現在の論文前到達点は、YAML IntentからPath選択、Agent telemetryによる再評価、strongSwan/VPP向けplan生成、Direct/Hub/Relay/VLANのroute表現、Failure/Recovery、Explain、再現可能な評価レポートまでである。SIGHUP reload、入力境界の安全性、有限Agent parallel socket受信、VICIのeventnetd接合、VPP Binary APIの低レベルtransportは実装済みである一方、Flow Preserve、VPPの版依存codec、無期限parallel service、FRR連携、IPsec復号後からVPP forwardingまでの単一packet本番pipeline、HAおよびGUIは引き続き将来課題である。
その後、state file復元について現在Intentのtraffic key、Pathのexplicit／candidate／fallback所属、Pathと全経由Nodeのadministrative stateを照合する境界を追加した。別Intent・候補外Path・無効化されたPath／Nodeは復元せず、`state-boundary`評価で再現できる。VLAN FIB tableの作成・sub-interface割当・route投入順序もCTestで固定し、Linuxビルドではstack protector、FORTIFY、RELROを既定有効にした。

22. 考察
本章では、現時点のPrototypeおよび検証結果から得られた知見を整理する。
定量評価は今後追加する必要があるが、初期実装を通じて、Ibukiの設計上重要な論点がいくつか明確になった。

22.1 Tunnel抽象化の有効性
初期実装では、Direct、Hub、Relayを個別の特殊処理としてではなく、PathとSegmentの組み合わせとして扱うことができた。
DirectはWaypointを持たないPath、HubやRelayはWaypointを持つPathとして表現できる。

このことは、拠点間通信経路をTunnel単体ではなく、複数SegmentからなるPathとして抽象化する設計が有効であることを示している。
特にHub Pathでは、site-a-hubとhub-site-bという複数Tunnelを一つのEnd-to-End Pathとして扱う必要がある。
この構成をControllerが選択対象として扱えたことは、Path Resource抽象化の妥当性を支持する。

一方で、Tunnel抽象化を過度に一般化すると、Backend固有の有用な機能を失う可能性がある。
例えばstrongSwanのVICIイベント、XFRM interface、VPPのFIB操作、FRRのBFD連携などは、それぞれ実装固有の性質を持つ。
これらをすべて隠蔽すると、上位Controllerは高度な制御を行いにくくなる。

したがって、Ibukiでは完全な同一Interfaceを目指すのではなく、共通操作とCapabilityの差を分ける設計が重要である。
共通操作として扱うべきものは、Tunnelの作成、削除、状態取得、Path検証、Forwarding反映などである。
一方、Flow Preserve、Hardware Offload、BFD連携、特定暗号方式、route-based IPsecなどはCapabilityとして表現するのが妥当である。

22.2 Path Transitionを状態機械として扱う意義
実装を進める過程で、Path変更を単なるroute更新やTunnel起動の列として扱うだけでは不十分であることが確認できた。
Direct Pathへ切り替える場合でも、Tunnel確立、swanctl load、CHILD SA initiate、VPP route投入、End-to-End疎通確認など、複数の段階が存在する。

これらの段階のうちどこで失敗したかによって、取るべき復旧動作が異なる。
Tunnel確立前に失敗した場合はCurrent Pathを維持すればよい。
Forwarding切替後に失敗した場合はRollbackが必要になる。
Commit後に対向拠点が切り替わっていない場合は、片側再適用またはRollbackを考える必要がある。

したがって、Prepare、Validate、Commit、Verify、Rollbackを明示したTransition Modelは、実装上も説明上も有効である。
特にExplain出力と組み合わせることで、Transitionがどの段階で成功または失敗したかを追跡できる。

22.3 Immediate、Graceful、Flow Preserveの位置付け
現時点のPrototypeでは、主にImmediateに近いTransitionを扱っている。
Target Pathを選択し、必要なruntime planを生成し、IPsec/VPPへ反映するという流れである。
これは実装が単純であり、初期検証に適している。

一方で、実運用を考えるとImmediateだけでは不十分である。
Path切替時にpacket lossやpacket reorderingが発生する可能性があり、長時間TCP sessionや遅延に敏感な通信では影響が大きい。

Graceful Transitionでは、旧Pathを一定時間保持し、Target Pathの検証後に段階的に移行することで、切替失敗時の復旧可能性を高められる。
Flow Preserveでは、既存Flowを旧Pathに残し、新規FlowのみTarget Pathへ移すことで、session continuityを高められる可能性がある。

ただし、Flow PreserveにはFlow識別、Flow table、Timeout管理、Forwarding ruleの分岐などが必要であり、実装負荷が大きい。
そのため、Ibukiの初期評価ではImmediateとGracefulを優先し、Flow Preserveは拡張評価として扱うのが現実的である。

22.4 Reconciliationの必要性
Controllerが生成したplanを適用しても、実際のOSS状態が常に期待通りになるとは限らない。
strongSwanではSAが削除される可能性があり、VPPではrouteが削除または上書きされる可能性がある。
また、Controller再起動後にはApplied Stateが失われ、Observed Stateを再取得する必要がある。

このため、IbukiではTransition Engineだけでなく、Reconciliation Runtimeが必要である。
Desired State、Applied State、Observed Stateを比較し、差分を検出し、必要な再適用またはRollbackを判断する仕組みが必要になる。

KubernetesのController Loopと同様に、Ibukiも最終的には状態を目標へ収束させる必要がある。
しかし、ネットワークPathでは中間操作が通信へ影響するため、単純にDesired Stateを再適用すればよいわけではない。
現在Transition中なのか、Commit前なのか、Commit後なのか、Rollback中なのかによって、再適用の意味が変わる。

したがって、ReconciliationはTransition Stateを参照して実行する必要がある。
この点が、一般的な宣言的状態管理をネットワークPath制御へ適用する際の重要な課題である。

22.5 管理ドメイン間協調の難しさ
Ibukiでは、将来的に複数Domain Controller間でPath Transitionを協調することを想定している。
しかし、複数Controller間で完全なAtomic Commitを保証することは難しい。
Control Channelの遅延、切断、片側Controllerの再起動、Policy拒否、片側だけのPrepare失敗などが起こり得る。

そのため、IbukiではデータベースTransactionのような完全な同時性を目指すのではなく、Timeout、Generation ID、冪等操作、Rollback Window、Current Path保持によって、安全な状態へ収束させる設計が妥当である。

この考え方は、本研究の新規性であるPath Transition Modelとも整合する。
Transitionを状態機械として管理していれば、片側がどの段階まで進んだかを記録できる。
その情報をもとに、再試行、Abort、Rollback、Manual Interventionを判断できる。

22.6 Explainabilityの有用性
Scenario HarnessでExplain JSONLを出力した結果、Path Selectionの判断理由を記録することの有用性が確認できた。
動的なPath制御では、通信が成功している場合でも、なぜそのPathが選ばれたのかを運用者が知る必要がある。

特にFallbackやEvaluated Selectionでは、単にselected_pathを表示するだけでは不十分である。
どのPathが候補だったか、どのPathがどの理由で除外されたか、どのmetricが比較に使われたかを記録する必要がある。

Explainは障害調査だけでなく、研究評価にも有用である。
同じscenarioを複数回実行した際に、Controllerが同じ理由で同じPathを選んだか、品質条件を変えたときに選択理由が変化したかを確認できる。

22.7 既存技術との差分
Ibukiは、Intent、Controller、Agent、VPP、FRR、Rollbackといった個々の概念を新規に発明するものではない。
ONOSにはIntent Frameworkがあり、KubernetesにはReconciliationがあり、flexiWANにはOSS SD-WANとしての実装が存在する。

Ibukiの意義は、これらの個別要素ではなく、標準Tunnelを限定された共通制御境界として扱い、Path SelectionとPath Transitionを実装横断的に比較できる研究基盤として構成する点にある。

特に、Path変更を最終状態更新ではなく、Prepare、Validate、Commit、Verify、Rollbackを持つTransitionとして扱う点が重要である。
これにより、経路選択アルゴリズムだけでなく、経路変更の安全性や通信影響を比較対象にできる。

22.8 現時点の限界
現時点のPrototypeにはいくつかの限界がある。

第一に、定量評価が未完了である。
疎通やcounter増加は確認できているが、Transition Time、Packet Loss、Packet Reordering、CPU使用率などの反復測定は今後必要である。

第二に、Tunnel Backend交換の評価が未完了である。
strongSwanを中心に検証しているため、WireGuard、GRE、VXLAN、Cloud VPNなど別Backendで同じTransition Modelをどこまで再利用できるかは未検証である。

第三に、Flow Preserveは設計段階であり、実装評価は未完了である。

第四に、Controller Federationは設計段階であり、複数Domain Controller間の実験は今後の課題である。

第五に、現在のruntime integrationは、IPsecとVPPを一つの生成planで連続制御できることを示しているが、復号後packetをVPP pipelineへ完全に統合する本番gateway pipelineまでは到達していない。

22.9 今後の評価で確認すべき点
今後の評価では、次を重点的に確認する必要がある。

ImmediateとGracefulの切替時間および通信影響の差
Rollback成功率とRollbackに要する時間
Direct障害からFallback完了までの時間
Control Plane障害時にActive Pathを維持できるか
Controller再起動後にObserved Stateから状態復元できるか
異なるTunnel Backendで上位Engineを再利用できるか
複数Domain Controller間でTransition Proposalを安全に実行できるか
Explain Logから判断過程を再現できるか

これらを確認することで、Ibukiが単なる設定生成ツールではなく、状態を持つイベント駆動型ネットワーク制御基盤として有効かを評価できる。

22.10 考察のまとめ
初期実装とsmoke testの結果から、標準TunnelをPath Resourceとして扱い、複数Pathの選択とruntime plan生成を行う設計は実装可能であることが確認できた。
また、Path変更を中間状態を持つTransitionとして扱う必要性も、実装上の操作順序と失敗時処理から明確になった。

一方で、研究としての主張を強めるには、定量評価、Backend交換評価、Transition Strategy比較、Rollback評価が不可欠である。
今後は、現在のPrototypeを基盤として、評価実験を反復可能な形に整備し、Ibukiの二つの中心的提案であるTunnel Resource AbstractionとExplicit Path Transition Modelの有効性を検証する。

23. 制約
Ibukiは、Path変更時の完全なPacket Loss Zeroを保証しない。
また、異なるTunnelをまたいだ完全なPacket Orderingも保証しない。
独自のPacket Duplication、Sequence Number、Reordering Protocolなどを導入しないためである。
さらに、すべてのネットワーク機器を直接制御できることも保証しない。外部APIや設定Interfaceを持たない機器では、strongSwanなどを動作させるSoftware Gatewayを別途配置する必要がある可能性がある。
IbukiはInternet全体のRouting Protocolを置き換えるものでもない。
また、初期的なController Federationでは、悪意のあるControllerを含むByzantine Fault Toleranceまでは対象としない。

24. 今後の展望
初期研究ではIPsec Tunnelを中心に扱う。
しかし、Tunnel Resource Abstractionが十分に一般化できる場合には、WireGuard、GRE、VXLAN、Cloud VPN、各種Cloud Providerが提供するTunnel Serviceなどへ適用できる可能性がある。
また、PathのWaypointとしてSecurity GatewayやCloud Gatewayを定義することで、Service ChainingやZero Trust、SASE、Edge Computingなどへの応用も考えられる。
一方で、Ibukiをルータ、Firewall、Switchなどあらゆるネットワーク機能を統合管理する汎用Controllerへ拡張することは、本研究の直接的な目的とはしない。
研究対象はあくまで、Tunnelと、それらから構成されるPathの選択および状態遷移に置く。

25. 結論
本研究では、標準Tunnelを共通のネットワークリソースとして扱い、その選択と状態遷移を宣言的かつイベント駆動で制御するネットワーク制御基盤「Ibuki」を提案した。
Ibukiは新しいVPN方式や独自データプレーンを開発するものではなく、IPsec、strongSwan、VPP、FRR、Linux networkingなどの既存技術を活用する。
本研究の中心的な提案は二点である。
第一に、個別実装の差異をAdapterへ局所化し、Capabilityとして明示することで、Tunnelを特定の機器やVPN実装への依存が少ない共通制御リソースとして扱うことである。
第二に、Path変更を最終状態への単純な更新としてではなく、Prepare、Validate、Ready、Commit、Verify、Rollbackという中間状態を持つTransitionとして扱い、異なるTransition Strategyを選択可能にすることである。
ONOSにはIntentに基づくネットワーク制御が存在し、Kubernetesには宣言的な状態収束が存在する。またflexiWANには、OSSを利用したSD-WAN、Controller-Agent構成、VPP、FRR、トランザクション、Rollbackなどが既に存在する。
したがってIbukiは、これらの個別概念の新規性を主張するものではない。
本研究が検証するのは、Tunnelという限定された共通境界を用いることで実装依存を低減できるか、そして通信Pathの変更を実装横断的なTransition Modelとして扱えるかという点である。
今後、提案モデルに基づく初期実装を行い、複数Path、複数Transition Strategy、障害注入、異なるTunnel Backend、複数管理Domainによる評価を通じて、本提案の有効性を検証する。

参考文献
[1] S. Kent and K. Seo, “Security Architecture for the Internet Protocol,” RFC 4301, Internet Engineering Task Force, Dec. 2005.
[2] C. Kaufman, P. Hoffman, Y. Nir, P. Eronen, and T. Kivinen, “Internet Key Exchange Protocol Version 2 (IKEv2),” RFC 7296, Internet Engineering Task Force, Oct. 2014.
[3] strongSwan Project, “strongSwan Documentation,” strongSwan Project, online. Accessed: Aug. 12, 2026.
[4] strongSwan Project, “vici Plugin — Versatile IKE Control Interface,” strongSwan Documentation, online. Accessed: Aug. 12, 2026.
[5] FD.io Project, “FD.io VPP Documentation,” Linux Foundation Networking, online. Accessed: Aug. 12, 2026.
[6] FRRouting Project, “FRRouting User Guide,” FRRouting Project, online. Accessed: Aug. 12, 2026.
[7] ONOS Project, “Intent Framework,” ONOS Documentation, online. Accessed: Aug. 12, 2026.
[8] Kubernetes Authors, “Controllers,” Kubernetes Documentation, The Linux Foundation, online. Accessed: Aug. 12, 2026.
[9] flexiWAN, “How flexiWAN Works — Architecture,” flexiWAN Documentation, online. Accessed: Aug. 12, 2026.
[10] flexiWAN, “About flexiWAN,” flexiWAN Documentation, online. Accessed: Aug. 12, 2026.
